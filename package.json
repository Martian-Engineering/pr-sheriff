{
  "name": "pr-sheriff",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CLI tool to identify superseded pull requests (scaffold).",
  "bin": {
    "pr-sheriff": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
